{
  "hooks": {
    "PreCompact": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PLUGIN_ROOT}/hooks/scripts/pre-compact-capture.sh\"",
            "description": "cortex: auto-capture a pre-compaction summary so context survives compaction"
          }
        ]
      }
    ],
    "SessionEnd": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "\"${CLAUDE_PLUGIN_ROOT}/hooks/scripts/session-end-summary.sh\"",
            "description": "cortex: write a session summary memory at session end"
          }
        ]
      }
    ]
  }
}
