---
findings: "## 2. Options-Considered structure check\nNo Options Considered / Option N structure present (this is a scoping/investigation plan, not a formal ADR). Advisory only, not applied as a hard auto-reject since artifact type is pre-ADR triage, but the two decisions with real trade-off space (resolveProject precedence source including loop.json parsing vs IPC/file from crew; issue-34 hook-trigger default) lack a documented rejected-alternative rationale. Should be added before coding resolveProject and the issue-34 hook.\n\n## 3. Strengths\n- Grounded against live code before proposing changes (verified flag/schema state per issue, correctly found issue-35 already mostly done and issue-33 real gap narrower than issue text implies).\n- Correctly identifies the write/read field asymmetry (remember writes metadata.project vs recall filters top-level project) as a verification-first blocking task rather than assuming daemon behavior.\n- Issue-34 default-OFF opt-in and deliberate-types-only default filter are the right instincts for a plugin writing into a user repo tree.\n\n## 4. Inversion findings (top 2 candidate designs)\nDesign A: resolveProject parsing crew .claude/loop.json directly (the plan chosen design). At 10x scale/session volume, if crew loop.json schema evolves (multi-project support, nesting) without a version marker astramem can detect, the parse silently returns undefined and falls through to the basename(cwd) fallback, reverting to exactly the split behavior the fix was meant to close, with zero operator signal because hooks are fire-and-forget and swallow errors.\nDesign B: alternative raised in plan own Open Questions, crew provider exposes resolved project via IPC or file for astramem to read. At 10x scale this requires crew to formally version a read contract (more upfront cross-repo coordination) but converges on one source of truth; failure mode becomes file missing or stale during version skew (detectable) rather than malformed silent parse (undetectable).\n\n## 5. Second-order effects\n6-month horizon: three independent project-scoping call sites already exist today (remember to metadata.project, recall to top-level project, ingest-transcript to top-level project_id via basename(cwd) in bash). Each new hook or subcommand is a fresh chance to forget resolveProject wiring; the plan own wiring list (remember, recall, recall hook) already omits ingest-transcript, the highest-frequency writer (fires on every SessionEnd, PreCompact, SubagentStop). First thing likely to break: a future call site ships with ad hoc basename(cwd) again, no enforcement forces resolveProject as the sole entry point.\n2-year horizon: if .claude/loop.json becomes a de facto stable read target for astramem-plugin without a formal contract, crew plugin loses freedom to evolve that file shape without silently breaking astramem, an unintended reverse dependency on an internal artifact never designed as a public interface. Assumption required for nothing significant to hold: crew commits to loop.json memory.project stability indefinitely, or a contract test exists in either repo; neither exists today (grep confirmed zero loop.json references in astramem-plugin).\n\n## 6. Confidence calibration\n1. Daemon maps ingest metadata.project to provenance.project (plan central blocking claim) - Confidence: low (plan itself flags as unverified). Evidence that would change my mind: daemon integration test result or astramem-local source read confirming the mapping.\n2. resolveProject walk-up-from-cwd for loop.json is safe inside a 15s-timeout fire-and-forget SessionStart hook - Confidence: medium. No bound stated on walk-up depth (repo root, filesystem root, symlink handling). Evidence that would change my mind: implementation showing a bounded walk-up (stop at git root) plus a cold-FS benchmark.\n3. Default-OFF is sufficient mitigation for issue-34 committing files into user repos - Confidence: medium. Addresses unsolicited-write risk but not redaction-freshness: scrub.ts runs at original ingest time, so atoms ingested under an older or foreign scrub ruleset are not guaranteed re-scrubbed before export-md commits them to git (a much higher-consequence sink than local-only recall). Evidence that would change my mind: export-md design re-running scrub.ts on recalled text before disk write.\n\n## 7. Critical findings (blocking)\n- Critical: resolveProject hidden coupling. Plan proposes parsing crew-owned .claude/loop.json with no shared schema or contract test; grep confirms zero existing references to loop.json in this repo. Consequence: silent drift if crew format changes, exactly the failure mode issue-33 is meant to close. Fix: add a contract test or fixture against crew loop.json shape, or drop loop.json parsing from v1 and ship flag/env-only precedence until crew commits to a stable format.\n- Critical: missing third project-scoping call site. ingest-transcript.ts (wired to SessionEnd, PreCompact, SubagentStop hooks) sets top-level project_id via its own basename(cwd) derivation in bash (session-end-summary.sh:22-26), entirely outside the plan wiring list. Consequence: this is the highest-frequency provenance writer; if not migrated to resolveProject, the split narrows but is not healed, session summaries keep diverging from remember/recall scope.\n\n## 8. Non-blocking findings\n- Wire-shape gating for IngestPayloadSchema is a one-shot manual probe-daemon-behavior check with no durable enforcement; IngestPayloadSchema (unlike TranscriptIngestPayloadSchema) carries no wire_version field, so future daemon-side remapping would silently re-diverge undetected. This repo already has a principled mechanism for exactly this (src/lib/wire-probe.ts, atom@N/retrieval@N/capture@N domain generations, commit e851f19). Route the schema decision through an atom@2-style generation bump instead of an ad hoc probe.\n- Issue-34 sequencing note: issue-34 depends not just on issue-33 resolveProject landing but transitively on issue-33 daemon-mapping verification succeeding without needing a cross-repo wire change; if that probe requires daemon coordination, issue-34 is blocked on an external timeline the Suggested Sequencing section does not call out as a risk.\n- Issue-35 confirmed correctly scoped and genuinely independent, SelectorResult.providerName already exists (src/contracts/selector.ts:21), no wire change, additive-only. No concerns beyond the two sub-options the plan appropriately leaves open.\n\n## 9. Open questions (plan defers, builders will need answers)\n- Is .claude/loop.json a stable, versioned contract astramem-plugin may depend on, or does crew need to expose project resolution through a dedicated interface (plan own Open Question 1)?\n- For issue-34, should export-md re-apply scrub.ts to recalled text at export time, or is ingest-time scrubbing treated as sufficient for git-committed output?\n- Walk-up bound for resolveProject loop.json search (stop condition) is unspecified.\n\n## 10. Recommendation\nProceed with issue-35 immediately (independent, low risk, confirmed). For issue-33/issue-34: revise resolveProject design to (a) either formalize the loop.json contract with crew or ship flag/env-only v1, and (b) include ingest-transcript.ts plus its hook shim in the wiring scope before calling issue-33 done. Route the IngestPayloadSchema top-level project decision through the existing wire-probe domain-generation mechanism rather than an ad hoc behavior probe. Issue-34 proceeds after issue-33 verification step lands; add scrub-at-export-time to the issue-34 design before implementation."
decision: approved_with_notes
---
# Review Result: Review Result

- Created: 2026-07-07T12:18:24.690Z
- Reviewer: reviewer
- Decision: approved_with_notes
- Summary: PASS-WITH-CONCERNS. 2 Critical findings: (1) resolveProject() plan depends on parsing crew-owned .claude/loop.json with no shared contract, real silent-drift risk; (2) plan wiring list omits the ingest-transcript hook path (a third, independently-derived project-scoping channel already in prod), so the split is narrowed but not healed.
- Evidence Checked:
  - Plan: .claude/artifacts/crew/runs/20260707-plan-issues-33-34-35.md. Confirmed against: src/contracts/wire.ts:18-33 (IngestPayloadSchema
  - no project/wire_version field)
  - src/cli/remember.ts:63-67 (metadata.project fold-in)
  - src/cli/recall.ts:73 (top-level project)
  - src/cli/ingest-transcript.ts:360 (top-level project_id
  - third scoping channel)
  - hooks/scripts/session-start-recall.sh:33-38 and session-end-summary.sh:22-26 (both basename(cwd)
  - no loop.json read)
  - src/lib/wire-probe.ts:1-48 (existing domain-versioning mechanism)
  - src/contracts/selector.ts:18-26 (providerName already present)
  - package.json:12 (./selector export). Repo grep for memory.project / loop.json: zero hits outside the plan doc itself.
- Files Reviewed: -
- Test Adequacy: -
- Non-Code Review: yes
- Risks: -
- Required Follow-up: -

