# Final Synthesis — parallel wave #33 / #34 / #35 / #26 / #27

**Repo:** astramemory-plugin @ main
**Date:** 2026-07-07
**Owner:** lead (crew) — 3-lane parallel worktree build + Wave 2 in-place

## Shipped (all merged to main, verified green)

| Issue | Commit(s) | What landed | Evidence |
|---|---|---|---|
| #35 | 3037856, merge 8f2ce76 | `ProviderHandle` (`MemoryProvider & { readonly backend }`); `stampBackend` on all 4 resolution branches; README + `providerName` doc | tsc 0, +4 selector tests |
| #33 | 7fef868, merge 4601e62 | `src/lib/project.ts` `resolveProject()` (flag→ASTRAMEM_PROJECT→config.project→basename); wired at remember/recall/ingest-transcript + 4 hook shims (dropped bash basename); `config.project` field | tsc 0, +project.test + cross-callsite consistency test |
| #34 | 47e2854 (in-place) | `astramem export-md` → `.claude/astramem/MEMORY.md` (default types decision,lesson; re-scrub at export; write-if-different); opt-in SessionEnd hook default-OFF | tsc 0, +8 export-md tests |
| #26/#27 | 5392fee, merge 5192897 | adopt `@astragenie/astramem-contracts@1.1.0`; canonical `MemoryType` union from `AtomV1Schema` (#27); extracted U0 adapter → `src/lib/wire-mapping.ts`; `ProviderCapabilities` on both providers | tsc 0, +wire-mapping tests |

**Integrated verification (final main):** `bunx tsc --noEmit` exit 0; `bunx vitest run tests/` → 1402 passed, 36 skipped, 0 failed; zero conflict markers.

## Slice-A daemon probe (gating decision, recorded)
Empirical: daemon maps `remember --project X` (metadata.project) → `recall --project X` (provenance.project) correctly (WRONGPROJ → 0 hits, probeproj → 1). ⇒ **#33 dropped the wire-shape change / atom@2 bump** — purely client-local. Removed the #33↔#26 wire.ts conflict, enabling true parallelism.

## Deferred / follow-ups
- **#26 discriminated union** (AtomV1Local / AtomV1Cloud over AtomV1Base): NOT implemented — published `@astragenie/astramem-contracts@1.1.0` ships only a unified `AtomV1`, no backend split. Blocked upstream; capabilities object shipped as the interim seam. Follow when the package publishes split variants.
- **#33 loop.json parse (v2):** deliberately deferred per architect C1 (drift risk) — behind a future contract test against crew's shape.
- **#28** (auto-distill low-signal) — daemon-side, not this repo. **#24** — blocked on daemon `domain@gen` emit.

## Merge-mechanics lessons
- Agent worktrees branched from the pre-session base (`3926504`), not session HEAD → Lane C (#26) 3-way-conflicted with #29 on local.ts/saas.ts (import regions only; resolved by union). Lane B (#33) auto-merged. **After merging a lane that added a dependency, run `bun install` on main** — the merge brings package.json/bun.lock but not node_modules; missing dep cascaded to `unknown`-type errors until installed.
- Wave 2 (#34) ran in-place (not worktree) because it needed #33's `resolveProject`, which the worktree base lacked.
